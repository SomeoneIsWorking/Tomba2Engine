# Copy to .env (git-ignored) and point at your Tomba! 2 disc image.
# Resolution order everywhere: CLI arg > this env var > drop-in *.chd in the repo root.
PSXPORT_TOMBA2_DISC=/path/to/Tomba! 2 - The Evil Swine Return (USA).chd
