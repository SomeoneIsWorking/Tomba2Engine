---
id: 2
title: Bucket-pickup cutscene SOFTLOCKS with pc_skip ON (default config)
status: doing
labels: [bug, pc-skip]
created: 2026-07-17
updated: 2026-07-22
---

USER live 2026-07-15: picking up a bucket initiates a cutscene that SOFTLOCKS under the default config (pc_skip=true). Likely a collapsed-init/wait fork in the cutscene trigger where the pc_skip shortcut branch fails to advance (a wait that never completes, or a phase-gate counter not bumped — cf. the pc_skip frame-counter-bump rule). Repro: replay the bucket-pickup pad capture default config, watch for the cutscene hang after bucket pickup; compare pc_skip=false (SBS Core A / MODE=skip) which should progress. Blocks gameplay progress.

**2026-07-22:** While chasing this, built tools/width_audit.py (compares every literal guest address access in game/ against the widths the GUEST uses in the real instruction stream) and it found 5 real defects in Engine::fieldRun - the pc_skip path, which is where every one of the reported bugs lives. The worst two: 0x800BF870 is the area-id BYTE (144x lbu / 1x sb in the guest, never wider) but was read as mem_r32 and compared ==0x15, which can essentially never be true since the read also covers bf871..73 - so that state transition NEVER FIRED under pc_skip; and it was WRITTEN with mem_w32, zeroing three adjacent live bytes every time state 6 ran. Also three mem_r32(0x800E7FEE) where the guest uses lh. All fixed, build clean. Whether any of this addresses the bucket dialog softlock is UNVERIFIED - needs a live retest.

**2026-07-22:** Dialog subsystem now partly OWNED rather than drafted: applyRenderMode (0x8007D0D0) was already port_check-PASS; advanceByte (0x8007C0D0) as drafted FAILED port_check (no 32-byte guest frame, 2 calls vs 3, store widths diverging at #4) so it was DELETED and re-derived byte-faithfully with port_gen.py as DialogTextStream::advanceByteGen, which port_check reports 0 FAIL (1 UNPROVABLE = the known 0xF8/0xF9 jump table the recompiler emits as a call). Wired at its real caller in beh_variant_overlay_lifecycle, replacing guest_leaf(0x8007C0D0). Behaviour is unchanged by the wiring itself (it is the gen body), so this does not claim to fix the softlock - it moves the dialog byte-stream advance into owned code so the actual state machine (FUN_8007D594, still substrate) can be ported next.

**2026-07-22:** Dialog box STATE MACHINE now owned: DialogBoxSm::step, byte-faithful port of FUN_8007D594 via port_gen.py (frame 32, spills r16@+24/r31@+28, 12 call sites), port_check PASS, installed via overrides::install WITH the shard_set_override setter (its only caller reaches it through the direct func_8007D594 thunk, not rec_dispatch). NOT YET EXERCISED: PSXPORT_DEBUG=ovhit reports 'registered but unreached' across both the bucket capture (1700f) and the save-sign capture (5200f) - neither replay actually opens a message box (the save-sign one softlocks before the menu appears). So this is verified-equivalent and cold, NOT proven working. Needs a capture in which a dialog actually renders; the bucket cutscene itself would do it.

**2026-07-22:** MEASURED: FUN_8007D594 is never called at all in real gameplay - ovhit reports native=0 oracle=0 (the thunk itself never entered) across the bucket capture, the save-sign capture, and 12000 frames of dark-screen-repro.pad. So the wide-RE survey's description of it as 'the box's own state machine' does not hold for the live in-game dialog path; it is plausibly on the pause/quit-menu side of that region. The port is correct (port_check PASS) but COLD and NOT a step toward this bug. Do not port more of 0x8007Dxxx on that assumption. What is actually needed: a capture with a message box ON SCREEN, then PSXPORT_DEBUG=dispatch to see which functions really run.

**2026-07-22:** MEASURED FACTS from the existing bucket capture (no new tooling): a dialog IS on screen during it - Panel::pushDialogGlyphs (0x8007CC00, owned, render-side rebuild) is dispatched 37x in the first 800 frames, and the pickup happens between f300 and f600 (bucket visible from f600 on). The dialog-region functions that actually RUN are 0x8007CC00 (owned), 0x8007E9C8 (owned, ScreenFade), and 0x8007E6DC/E8DC/E938/E998/0x8007FCC8/0x8007FD54 - all of which have byte-faithful leaf_ ports sitting UNWIRED (ORPHAN). FUN_8007D594 and the 0x8007C0D0 advance are NOT among them. DEAD END for isolating the box logic: PSXPORT_DEBUG=dispatch stamps every line f0, so functions cannot be correlated to the frames where the dialog is up - do not retry that correlation without fixing the channel's frame stamp first. The capture does NOT contain the softlock (user was playing normally), so the bug itself is still not reproduced on my side.

**2026-07-22:** Dialog-path ownership now: Panel::pushDialogBackdrop (1556 hits), UiSprite::compose (1018), UiSprite::drawFromTable + drawFixedDef152 (9 each), LoadingText::draw (1) - all RE'd into named readable code, all port_check PASS, all confirmed live by ovhit on this capture. The drawing side of the dialog is owned; the softlock is a LOGIC problem, so the next targets are whatever drives the box's lifetime, not more emitters.
