---
id: 60
title: SEQUENCE softlock (live capture) — game keeps running, sequence never advances
status: doing
labels: [bug, pc-skip]
created: 2026-07-23
updated: 2026-07-23
evidence: docs/reference/issues/issue60_sequence_softlock.png
---

USER 2026-07-23, cut live from their running session. A SEQUENCE softlock: the game keeps advancing (frame counter climbs normally, ~30fps, no crash, no recomp-MISS) but the sequence never progresses and control is not returned. Captured state at the lock: stage=GAME(0x8010637C), sm[0x48]=2, scene-active(0x800BE258)=2 — i.e. a normal in-field scene state, not a transition. Screenshot: Tomba on a grassy slope by a tree with a bird/parrot enemy just above him (docs/reference/issues/issue60_sequence_softlock.png).

REPRO: replays/bugs/sequence-softlock-2.pad, 7049 frames from boot. Headless:
  { echo "run 7100"; echo quit; } | PSXPORT_REPL=1 PSXPORT_VK_HEADLESS=1 PSXPORT_NOAUDIO=1 PSXPORT_PAD_REPLAY=replays/bugs/sequence-softlock-2.pad ./scratch/bin/tomba2_port
Do NOT also issue `newgame` — a pad replay starts at boot frame 0 and it desyncs.

THIS IS THE FOURTH SOFTLOCK OF THE SAME FAMILY. Read these first (`python3 tools/findings.py softlock`): #2 bucket-pickup cutscene (pc_skip ON), #5 save-sign inspect (CLOSED), #58 fisherman dialog. They share a shape — an interaction/script sequence parks forever while the frame loop keeps running.

STRONGEST PRIOR, CHECK BEFORE ANYTHING ELSE — kanban #50: under pc_skip the GAME task is a FLAT setjmp task, so a cooperative FUN_80044BD4 wait reached from inside the pc_skip GAME frame has its C++ stack DISCARDED by the longjmp; everything after the wait silently never runs. "A sequence parks forever while the loop keeps ticking" is exactly that signature, and it is why these are pc_skip-exclusive. #50's general fix (PcScheduler::spawnAndWait running the body inline on a flat task) landed 2026-07-23 — SO FIRST, RE-RUN THIS REPLAY ON CURRENT MAIN. If it no longer locks, this was #50 and the card closes. If it still locks, that is the finding: #50 deliberately did NOT convert four `native_area_load_bd4` flag==1 fire-and-forget sites, whose spawned task still runs flat — the remaining half of the same disease, and the prime suspect.

DIAGNOSIS (state, not crash — the process keeps running, so crash-checking proves nothing):
  - `PSXPORT_DEBUG=sched` — does "caught a GAME substate yield" fire at the lock frame? That is the #50 truncation signature and the single most informative probe.
  - `PSXPORT_DEBUG=seq` for the script/sequence state; DialogBoxSm (game/ui/dialog_box_sm.cpp, guest FUN_8007D594) and ScriptInterp for where it parks — #2's investigation used step counts + script-op tracing to pin the exact parked op, reuse that method.
  - REPL `watch <addr>` + PSXPORT_CW_BT=1 for a store backtrace on whatever should advance and does not.
  - The bird/parrot enemy is right next to Tomba in the capture; check whether an enemy/AI behaviour is the sequence owner (the beh_* A/B work in #10/#51 found real defects in rebuilt behaviour bodies, and 0x8013C9C0 / 0x8011D988 / 0x80121978 are still open divergences).

**2026-07-23:** 2026-07-23 agent: STILL REPRODUCES on current main (c5c5e1d). #50 is NOT the cause — PSXPORT_DEBUG=sched never fires 'caught a GAME substate yield'. Not a render bug (identical under PSXPORT_RENDER_PSX=1). Not the beh_* rebuild class — forcing ALL 65 BehaviorDispatch handlers to gen (PSXPORT_BEH_SUBSTRATE=<all>) still locks with byte-identical position. ROOT CAUSE LOCATED, NOT FIXED: cutscene-mode byte 0x1F800137 is stuck at 1 forever (it is 0/2 in healthy free-roam), because the scene's two scripts DEADLOCK on script op 0x04 (handler FUN_8004201C = a two-party rendezvous on the scene-flag array at 0x800BF9B4). Both actors use slot 55 (0x800BF9EB): object 800FC9E0 (beh_node3_router -> ov_a00_gen_8011C674, cursor 0x80148AF4) wrote 2 and awaits 1; object 800FC7D0 (beh_area_event_dispatch -> ov_a00_gen_80128BC0, cursor 0x80149DA0) wrote 1 and awaits 3. The byte is stuck at 2 — neither can fire. The handshake completes three times first, then loses a step. NEXT LEAD: the two actors are stepped from DIFFERENT per-frame walks in the same pc_skip frame — A via Engine::fieldRun->fieldFrame->ObjectList::walkAll, B via Engine::fieldRunX->fieldFrameX->TransitionState3::walkOnce — so walk ORDER/COUNT divergence drops exactly one handshake step. Full writeup + instrument caveats: docs/findings/scene.md 'SEQUENCE softlock #60'.

**2026-07-23:** 2026-07-23 #50 PRIOR FALSIFIED; ROOT CAUSE LOCATED; NOT FIXED (no bandaid shipped). Still reproduces on current main — verified BY STATE not exit code: at f7100 Tomba does not move under 240 frames of held right then 240 held left, position 0x800E7EAC byte-identical across all three samples. PSXPORT_DEBUG=sched NEVER emits 'caught a GAME substate yield', so the #50 truncation signature is absent and the four flag==1 fire-and-forget sites are NOT implicated. RULED OUT, each measured (do not re-chase): not render (identical lock under PSXPORT_RENDER_PSX=1); not the beh_* rebuild class (#51) — forcing ALL 65 BehaviorDispatch handlers to their gen bodies still locks with position byte-identical; individually force-genned with no effect: areaSeasidePerframe 0x80113C5C, beh_node3_router 0x8011CBD0, beh_area_event_dispatch 0x80071A3C, ScriptInterp::init 0x80040CDC, outerTransitionGate 0x80053E50, mode0ActionGate, mode0WalkHandler. ROOT CAUSE: the field is healthy (sm[0x48]=2 4a=1 4c=2 4e=1; Tomba's outer state G+4=4 dispatching the normal walk handler every frame) but the CUTSCENE-MODE byte 0x1F800137 is stuck at 1 forever (0 or 2 in healthy free-roam) — same signature as #2. Cut-mode 1 clears when the scene script finishes, and BOTH scene scripts are parked on script op 0x04 (FUN_8004201C), a TWO-PARTY RENDEZVOUS on the scene-flag array 0x800BF9B4: phase 0 writes flags[obj+0x72]=obj+0x74, phase 1 spins until flags[obj+0x72]==obj+0x76. Both actors use SLOT 55 -> 0x800BF9EB: actor 800FC9E0 (beh_node3_router -> ov_a00_gen_8011C674, cursor 0x80148AF4) writes 2 and AWAITS 1; actor 800FC7D0 (beh_area_event_dispatch -> ov_a00_gen_80128BC0, cursor 0x80149DA0) writes 1 and AWAITS 3. The byte is stuck at 2 — A waits for 1, B waits for 3, deadlock. A cw watch shows the handshake completing three times (2,1,2,1,2,1,2) then going silent: the scripts alternate correctly then LOSE EXACTLY ONE STEP. NEXT LEAD (unverified): the two actors are stepped from DIFFERENT per-frame walks in the same pc_skip frame — A via fieldRun->fieldFrame->ObjectList::walkAll, B via fieldRunX->fieldFrameX->TransitionState3::walkOnce (host backtraces) — so a walk order/count divergence there would drop one handshake step. Forcing the flag would be the banned bandaid and was correctly NOT done.
