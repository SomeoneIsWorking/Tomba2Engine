---
id: 8
title: Water-pump seesaw: Tomba's weight doesn't pull it down when grabbed while climbing (pc_skip ON)
status: done
labels: [bug, pc-skip]
created: 2026-07-21
updated: 2026-07-30
---

**2026-07-21:** Repro captured LIVE via the new dbg-server 'padrec save': replays/bugs/seesaw-weight.pad (6668 frames, from boot, NO memory-card load - user confirmed - so it is self-contained). Live probe while the user held the seesaw grabbed: an 'ents' diff over 4s shows NOTHING on either pump assembly moving (only Tomba's idle jitter at 800EE0D0 and one unrelated mover at 800FD118). Beam is genuinely static, matching the report.

**2026-07-21:** The two pump assemblies (left x~5.4-5.9k, right x~6.7-7.1k) are each 4 nodes: h=8012EB54 beh_substate_edge_orchestrator (12 cmds - the beam assembly), h=80136D9C beh_pure_inner_dispatch, h=80125E0C beh_pure_substate_dispatch, t=05 h=8004C238 beh_visibility_gate_dispatch. All are natively-owned DISPATCHERS, but 8012EB54 still has 12 still-PSX leaves (0x8012E8A8 8012ED84 8012F494 8012F5B4 8012FD88 80130524 80130AC4 80131134 801313C4 801316CC 80146348 8018C820). Per CLAUDE.md do NOT debug the divergence until that chain is owned end-to-end - grow ownership there first.

**2026-07-21:** DEAD END (method): comparing the pad replay across exec paths is INVALID. Same replay frame under pc_faithful vs PSXPORT_GATE=1 puts Tomba in completely different map locations - the two paths consume different numbers of pad frames during boot, so a capture desyncs on the other path. Evidence scratch/screenshots/seesaw_pc_vs_gate.png. Use SBS (lockstep, same input) for path comparison - but note SBS forces mPcSkip=false on both cores, so it does NOT exercise the pc_skip fork this bug is reported under.

**2026-07-21:** DEAD END (tool): 'provat' is blind under pc_render - it attributes GP0/VRAM writes, and pc_render never writes guest VRAM, so every probed pixel reads '<never written>'. Cannot use it to identify which node drew the beam.

**2026-07-21:** CORRECTION to the provat note above: that was NOT a new dead end - docs/gfx-debug.md:58 already said provat is blind to VK-teed polys and to use PSXPORT_PRIMAT instead. I re-derived a documented fact by not reading first. The listing at gfx-debug.md:22 has been fixed to carry the caveat where provat is introduced. Next time: PSXPORT_PRIMAT="x,y" is the attribution tool under pc_render.

**2026-07-21:** ROOT CAUSE of the replay not reproducing (a TOOLING bug, not the game bug): the dbg-server pause loop called Pad::serviceFrame() every 15ms while frozen, and the record/replay frame index lives inside serviceFrame - so the pad-frame clock advanced while the game did not. The user's session was paused (P key) during capture, so seesaw-weight.pad contains masks recorded against a frozen game; replaying consumes them while the game runs -> everything after the pause is shifted. FIXED via Pad::pumpHostInput() (verified: padrec 5720->5720 over 6s paused, advances on resume). CONSEQUENCE: the current seesaw-weight.pad is unreliable past the first pause and must be RE-RECORDED. Also SUPERSEDES the earlier 'replays desync across exec paths' note - that observation is explained by this bug and must be re-tested with a clean capture before being believed.

**2026-07-21:** RETRACTION (USER caught it): the previous note's claim that seesaw-weight.pad is corrupt and must be re-recorded is WRONG. The replay DOES reproduce the grab - zooming the probe screenshot shows Tomba hanging on the pump; I had mistaken an NPC for him at 320x240 and concluded 'on the ground, not grabbing'. Everything I derived from that (capture corrupt, cross-path desync explained) was speculation asserted as fact. The pause-clock defect is separately real and measured, but implies nothing about this capture. USABLE STATE now established: replay to pad frame ~6424 and pause -> Tomba grabbed, both beam nodes node[4]=1 node[5]=0, nothing moving. That is the state to investigate from.

**2026-07-21:** MECHANISM ESTABLISHED (docs/findings/ai.md): consumer FUN_801308e0 turns node[0x2b] (contact index) into node[0x48]=0xe000, the weight. WWATCH over the whole replay: node[+0x2b] written 6585x per node, ALWAYS 0, only by clearers (80130D5C/801316CC/80146348/80084A80). Nothing ever writes a nonzero contact value - the producer never fires. Consumer healthy. NEXT: find the guest producer that stamps a contact index into a struck object's +0x2b; unverified lead = player surface-probe cluster FUN_80024448 / caller 0x8005D530 (not yet ported).

**2026-07-21:** CALL CHAIN MAPPED (all from the grab-state dump + generated/): player G+0x158 (attach ptr) = 800FB960 = node[3]=1, so THAT is the object Tomba hangs on (verified, not assumed - found by scanning RAM for references). It is written by FUN_80057a68 as 'player[0x158] = FUN_80024548(player,0)'. FUN_80024548 (the ride scan) calls FUN_80024448 - the surface probe RE'd 2026-07-21 - so yesterday's lead is now a confirmed call edge. NEITHER writes +0x2b. FUN_80024448's callees are 80024AF0/80046A44/80048654 (unwalked). The overlay per-candidate test FUN_80111a00 stashes the winner in scratchpad 0x1F800084 and is NOT in generated/ (overlay). Oracle run (GATE=1) also shows ZERO nonzero +0x2b writes - INCONCLUSIVE, because it was not established that the oracle leg reaches the grab at all. NEXT: find any object anywhere whose +0x2b goes nonzero (proves the producer exists and works), then diff against this one.

**2026-07-21:** PRODUCER FOUND + one hypothesis killed. Producer = FUN_80111304(player,obj): overlap test, then obj[0x2b]=1, then obj[0x2b]=2 when within (player[0x80]+obj[0x80]+0x3c) - value 2 is exactly the weight case in the consumer. Chain: handler table 0x8009D1D4[area_id] -> FUN_80113C5C -> FUN_801130C4 -> FUN_80111304. Dispatcher = gen_func_80022A80 = NATIVE Engine::modePerFrameDispatch; under pc_skip it swaps 0x80113C5C for Behaviors::areaSeasidePerframe, an admitted 'result-equivalent, not byte-exact' REBUILD - the prime suspect for a pc_skip-only bug. HOWEVER the specific guard I suspected (item[0xC]==9 + growthFlags&0x8200) is IDENTICAL in the native postInteractWalk and in guest FUN_801130c4, so that is NOT the defect - hypothesis killed. Beam is class 4, so it is not stamped via the class-9 path in EITHER; its stamp must come from one of the other 104 SB-into-+0x2b sites. Aux list count at 0x1F800144 reads 0 in the .spad at the dump instant (unresolved whether that is frame-phase). NOTE: WWATCH cannot see scratchpad (control-verified) - do not use it for scratchpad questions.

**2026-07-21:** FIX LANDED (needs live confirmation): Behaviors::areaSeasidePerframe read the ride/attach pointer G+0x158 as SIGNED 16-BIT ('mem_r16s(0x800E7FD8) < 2') where the guest does a 32-bit UNSIGNED test (lw + sltiu, verified in the instruction stream at 0x80113CC0). Attached, (int16)0x800FB960 = -18080 < 2 is TRUE so FUN_8011334C fired exactly where the guest suppresses it; unattached both agree, which is why it was invisible until something is grabbed. Fixed to mem_r32(...) < 2u. CONTROLLED A/B on the same replay watching node[+0x48] (the weight): pre-fix 0 nonzero writes across all 12 nodes over 6680 frames, post-fix 448 on node index 1 - the exact node the attach pointer targets. NOT VERIFIED that the beam visibly sinks: the fix changes behaviour so the open-loop replay no longer reaches the grab at the same frames. USER: please retest live. Note +0x2b is still never stamped, so the FUN_801308e0 contact path stays unexercised - if it still misbehaves, that is the next thread.

**2026-07-21:** CORRECTION to the previous note: the 0->448 A/B does NOT show the fix addresses the grab. All 448 nonzero weight writes fall in frames 315-748; the grab is ~frame 6400. And the post-fix dump at 6600 has G+0x158==0, i.e. the diverged replay never reaches a grab late in the run. The signed-vs-unsigned defect is still REAL and proven at the instruction level (lw+sltiu vs mem_r16s) - that stands on its own and is worth fixing regardless. But its connection to this symptom is UNESTABLISHED, and the pad replay structurally cannot establish it. Live retest is the only way.

**2026-07-21:** CONTACT-STAMP THREAD (grounded, from the grab dump + .spad): the beam is class 4 / type 0 / visible and IS present in the class-4 queue (ptr 0x1F80014C, count 0x1F800152, 11 entries, beam at index 5). So it is queued correctly - queueing is NOT the failure. interact_scan legitimately skips it (its activatable-type list has no type 0); that is the button-activation path, not the weight path. The aux walk in areaSeasidePerframe consumes the CLASS-5 queue C (0x1F800154/0x1F80015C, empty here), so FUN_80112A60 is not the beam's stamper either - I earlier mis-attributed 0x80077F24/F30 to enqueueVisibleClass4; they are in FUN_80077EFC (queue C). Of the 105 sb-into-+0x2b sites, 22 write the literal 2 (the weight case); the 13 MAIN.EXE functions containing them are ALL UNOWNED substrate. So the stamp is original code, not a mis-port - the defect is upstream REACHABILITY of those functions, which is where to look next.

**2026-07-21:** ★ GATE FOUND: FUN_80113700 (a trio leaf dispatched by areaSeasidePerframe) IS the contact stamper - it walks the 0x800F2738 object list and, per object with obj[0x2b]==0, walks QUEUE A and calls the per-type handlers containing the sb-into-+0x2b sites (incl. the literal-2 ones). Its entire body is gated on 'if (_DAT_1f800144 != 0)' = queue A's count, and in the grab dump that count is 0 -> immediate return -> nothing ever stamped. Consistent with every +0x2b measurement. NOT a dump-phase artifact: at the same instant class-4 queue = 11 entries while queue A and queue C = 0, and all three are filled by the same cull pass. NEXT: why are class-2/9 objects not enqueued into queue A? Fillers are Cull::enqueueQueueA (FUN_80077E7C) and Cull::enqueueByClass (FUN_8007703C, class 2/9 -> queue A) - BOTH NATIVELY OWNED (game/render/cull.cpp), so that is the first owned code on the path and the prime suspect.

**2026-07-21:** Queue-A fill path is ENTIRELY natively owned (objListWalk1 / enqueueByClass / Cull::decide+performBaseCull / enqueueQueueA / initTypedPools). Our queue addresses are VERIFIED CORRECT against the guest instruction stream (FUN_80077E7C offsets 316/324 = 0x1F80013C/0x1F800144 cap 24; FUN_80077EBC 328/336 = 0x1F800148/0x1F800150 cap 40) - enqueueQueueA is not mis-addressed. Cull::decide gates every queue assignment on *(u32)0x1F800080==0; the grab dump has 0x1F800080=0x467 and A/B/C all zero. NOT YET A FINDING: the dump is at pad-service phase and those scratchpad words are reused there (0x1F800084 reads 0x6C40), so empty queues may be a phase artifact; WWATCH cannot watch scratchpad. NEXT: sample 0x1F800144 and 0x1F800080 INSIDE areaSeasidePerframe right before it dispatches LEAF_TRIO_113700. Zero there = the gate is genuinely shut and the cull path is the bug; non-zero = this branch is a dead end.

**2026-07-21:** DEAD END RESOLVED + REAL GATE FOUND. The 'queue A is empty' branch was a PHASE ARTIFACT: the new registered 'cullq' probe (samples inside areaSeasidePerframe right before the trio dispatch) shows queue A at 11-12 entries in nearly all 6582 samples and the cull gate 0x1F800080==0 (open) in 6580 - Cull::decide/enqueueQueueA are NOT the bug. The REAL gate is FUN_80113700's SECOND check: 'LW r17,0x2738(0x800F0000); BEQ r17,r0' - the object-list head at 0x800F2738. The cullq probe walks that chain and finds length 0 in 6570/6582 samples, so the stamper returns immediately every frame. That is exactly why +0x2b is only ever written 0. The head is zeroed by FUN_8007A810 (from Pool::init, ours) and filled by FUN_8007AB44, whose 6 callers (80068FBC 80069300 8006AE28 8006BB6C 8006C478 8006C59C) are ALL unowned substrate, in the same band as the literal-2 stamp fns. NEXT: callers of those six - the first natively-owned one that fails to reach them is the defect.

**2026-07-21:** ⚠ SELF-DOUBT ON THE LATEST CONCLUSION: FUN_8007A810 (pool init) sets head 0x800F2738=0 and tail 0x800F23A0=0 and builds a 4-node FREE list; FUN_8007AB44 only UNLINKS. A scan that understands lui+store AND lui+addiu-into-register finds NO other writer, and gp-relative stores cannot reach it (gp=0x800BE0D4, needs 0x34664 displacement > 32KB). So the active list starts empty and can only shrink - IN THE GUEST TOO. That means FUN_80113700's outer walk is a no-op on real hardware as well and is NOT the seesaw's weight mechanism; its null head is probably NORMAL, not 'the gate that is shut'. The chain from 'FUN_80113700 is the stamper' onward is suspect. Still solid: the consumer RE (FUN_801308e0 -> node[0x48]=0xe000), +0x2b never stamped, and the queue/class facts. RESTART POINT: find which of the 105 sb-into-+0x2b sites can fire for a class-4 TYPE-0 object, working from the object's own handler chain, NOT the aux/queue walks.

**2026-07-22:** ⚠ DUMP CONTAMINATION (self-caught 2026-07-22): PSXPORT_PAD_DUMP_AT writes a FIXED filename (padram_<frame>.bin), so the post-fix run at frame 6600 silently overwrote the pre-fix GRAB dump. Both files now hold G+0x158=0 (NOT grabbed). Every analysis after that point - class-4 queue contents with the beam at index 5, node[0x2b]/+0x29/+0x48 'at the grab frame', empty aux lists, null object-list head 0x800F2738, scratchpad gate values - was read from a NOT-GRABBED dump while being described as grab-state. Those specific conclusions must be re-derived from a dump asserted to have G+0x158 != 0. STILL VALID: the whole-run WWATCH measurements (+0x2b never non-zero across a pre-fix replay that DID grab, verified visually at pad 6424), and all the RE from Ghidra/generated. Also: tools/drive_to_grab.py added - closed-loop, gates on the attach pointer instead of frame numbers, and it correctly reports that the CURRENT build never grabs with this replay (attach pointer 0 through 19381 pad frames).

**2026-07-22:** RE-DERIVED from a VERIFIED grab dump (scratch/bin/grab_prefix_6600.bin, asserted G+0x158=0x800FB960 before use). The substantive facts HOLD: beam 800FB960 while genuinely held has +0x2b=0, +0x29=0, +0x48=0, +0x4e=0, node[5]=0, tilt=0xF47 pinned at its clamp. Beam IS in the class-4 list (index 3 of 8, not 5 of 11 as the contaminated read said) - queueing confirmed correct. EVERY entry in that list has +0x2b=0, so nothing in the scene is stamped, not just the beam. Stamper outer-list head/tail both 0, consistent with the pool-init reading. Only incidental numbers changed; no conclusion reversed.

**2026-07-22:** ✅ USER CONFIRMED FIXED (2026-07-22): 'It's already working now' - the seesaw sinks under Tomba's weight again. The signed-16-bit read of the ride/attach pointer G+0x158 (mem_r16s instead of the guest's 32-bit unsigned lw+sltiu) was the whole bug: attached, (int16)0x800FB960 = -18080 < 2 is TRUE so FUN_8011334C fired exactly where the guest suppresses it; unattached both agree, which is why it was invisible until something was grabbed. NOTE the +0x2b/contact-stamp thread was a NON-PROBLEM - +0x2b is never stamped non-zero and that is normal; do not resume it.

**2026-07-29:** 2026-07-29: FOUR of the twelve still-PSX leaves this card names as the blocker are now natively owned and SBS-gated: 0x8012F494 (substate0Tick), 0x80130AC4 (visibilityGate), 0x80131134 (armPendingChildPair), 0x801316CC (tickChildOscillators) — game/ai/substate_edge_native.cpp, port_gen bodies, 50/50 A/B-identical with balanced ovhit. Three REPLACED hand drafts that were defective (8, 5 and 6 defects respectively). EIGHT REMAIN: 0x8012E8A8, 0x8012ED84, 0x8012F5B4, 0x8012FD88, 0x80130524, 0x801313C4, 0x80146348, 0x8018C820. Honest note on method: these were selected off a dispatch histogram WITHOUT knowing they were this chain; the connection was made afterwards by running ents at pad frame 6424 and matching node positions (800FB858 x=5562, 800FB960 x=6678, both cmds=12) to this card's own pump coordinates. Orientation recorded in docs/findings/ai.md.

**2026-07-29:** 2026-07-29: THE STATED BLOCKER IS CLEARED. This card said 'do NOT debug the divergence until that chain is owned end-to-end - grow ownership there first' and listed 12 still-PSX leaves of FUN_8012EB54. ALL TWELVE are now natively owned and SBS-gated: 0x8012E8A8, 0x8012ED84, 0x8012F494, 0x8012F5B4, 0x8012FD88, 0x80130524, 0x80130AC4, 0x80131134, 0x801313C4, 0x801316CC, 0x80146348, 0x8018C820 (game/ai/substate_edge_native.cpp, port_check 12 PASS, SBS 50/50 A/B-identical). A recdep sweep confirms none of the twelve still reaches the substrate. CAVEAT ON COVERAGE: three of them (0x8012FD88, 0x80130524, 0x801313C4) are recorded UNTESTED — this replay never enters those sub-states, so their gate is vacuous. Six hand drafts were replaced along the way, all defective. Field vocabulary for the class is in game/ai/assembly_node.h; what the object IS (the seaside water pumps) is in docs/findings/ai.md.

**2026-07-29:** 2026-07-29: THE CONSUMER ITSELF IS NOW OWNED. FUN_801308E0 — the function this card identified as turning node[0x2b] (contact index) into node[0x48] (the weight) — is ported as SubstateEdgeLeaves::contactWeightApply (game/ai/substate_edge_native.cpp), port_check PASS, SBS 50/50 A/B-identical, ovhit 16680/16680 balanced. Reading the body confirms this card's mechanism directly: it writes node+0x2B (43) and node+0x48 (72, halfword) plus the sub-state, node+6 and the mode byte, and calls 0x80074590 twice. Two of the CLEARERS this card measured (0x801316CC, 0x80146348) are also owned now, and both write literal zero to +0x2B — consistent with the card's finding that the value is always 0. The whole contact->weight path is therefore native. NOTE THIS DOES NOT FIX ANYTHING: the producer (FUN_80111304) still never fires for the beam, which is class 4. The consumer is healthy and starved. What changed is that the starvation is now readable in native code rather than inferred across a substrate boundary.

**2026-07-30:** 2026-07-30: THE GATE IS THE AUX-LIST COUNT, NOT THE OBJECT CLASS. Read both gen bodies: the producer FUN_80111304 writes obj[0x2b]=1 only when rec_dispatch(0x8002300C) — an overlap test — returns nonzero, and its caller FUN_801130C4 reads a candidate-list pointer from scratchpad 0x1F80013C and a COUNT from 0x1F800144, copies the count to a working counter at 0x1F800182, and branches PAST THE WHOLE LOOP when the count is zero. So with that count at 0 the producer cannot fire for any object and the beam's class 4 is irrelevant to this gate — which makes this card's own earlier note ('Aux list count at 0x1F800144 reads 0 in the .spad at the dump instant, unresolved whether that is frame-phase') the actual thread, not a side detail. NEXT is a MEASUREMENT not an RE: sample 0x1F800144 across frames rather than at one dump instant. WWATCH cannot see scratchpad (control-verified on this card), so it needs a REPL read or a purpose-built probe. Still unread: who writes 0x1F800144, and whether the beam would pass the 0x8002300C overlap test at all. Full structure in docs/findings/ai.md.

**2026-07-30:** 2026-07-30 MEASURED: the aux-list count is zero for the WHOLE replay, not at one frame phase. Sampled scratchpad 0x1F800144 (the byte ov_a00_gen_801130C4 reads and branches on) every 500 frames from f500 to f6500 across seesaw-weight.pad, spanning the grab at ~6424: 0x00 at all 14 samples. That resolves this card's own open note ('reads 0 in the .spad at the dump instant - unresolved whether that is frame-phase') — it is not frame-phase, the producer loop is skipped throughout. Recorded as claim C023 with a falsifier. OBSERVATION worth chasing, not a conclusion: the list POINTER at 0x1F80013C looks live (0x800F2410) and the neighbouring byte at 0x1F800146 DOES vary (0x07 at f500, 0x0C at f1000, 0x06 from f1500 on) — so something list-shaped is maintained nearby while the byte the producer reads stays zero. Whether 0x146 is the real count and 0x144 the wrong field, or 0x144 is right and simply never populated, is NOT established: the next step is finding what writes those bytes. Note WWATCH cannot see scratchpad, so that needs a different probe.

**2026-07-30:** 2026-07-30 CORRECTION — MY OWN EARLIER NOTE THIS SESSION WAS WRONG. I claimed (claim C023, now FALSIFIED) that the contact producer FUN_80111304 is skipped for the whole replay because the aux-list count at scratchpad 0x1F800144 is zero. The count IS zero, measured across 14 samples — but that gates only ONE of the producer's EIGHT callers (0x801130C4; the others are 0x801131A4/0x801131B8/0x801131CC/0x801132C8/0x801132D8/0x801132EC/0x80113314, mid-loop entry clones). Wiring the producer natively and reading ovhit settles it: native=1151 oracle=1151 over 1500 frames — THE PRODUCER RUNS, about once per frame. So 'the producer never fires' is false as an explanation. The question moves back to the producer's OWN gate: it writes item+0x2B=1 only when the overlap test 0x8002300C returns nonzero. With 1151 calls and no nonzero contact index ever observed, either that overlap test fails every time for these objects, or a clearer zeroes the stamp within the same frame (this card already measured +0x2b written 6585x per node, always 0, by clearers — two of which are now natively owned). NEXT: instrument the overlap test's return, not the call count. 0x80111304 is now native (game/ai/contact_stamp.cpp), so that probe can go in native code.

**2026-07-30:** 2026-07-30 MEASURED, and it kills the remaining hypothesis: THE OVERLAP TEST NEVER PASSES. Added a host-only counter inside the now-native producer on the return of rec_dispatch(0x8002300C) — the gate that decides whether item+0x2B=1 is written. Result: passes=0 out of 6,000+ calls across the whole replay, on BOTH the default pc_skip path and the faithful PSXPORT_PC_SKIP=0 path. So the stamp is never written because the gate never passes — it is NOT that the producer is skipped (falsified C023, it runs ~1151 times per 1500 SBS frames) and NOT that a clearer wipes the stamp (there is no stamp to wipe). The question is now one layer down: why does 0x8002300C return zero? Its inputs here are the item radius at +0x80 scaled by 4 plus the caller's a0/a1 — the next probe measures those INPUTS rather than doing more RE on the producer. Diagnostic is PSXPORT_DEBUG=contact, host-only, no guest writes. Observation not conclusion: all 12 sampled reports show the same item 0x800FC8D8 with G=0x800E7E80, but the counter logs only every 500th call so that does not establish the producer sees one item.

**2026-07-30:** 2026-07-30 ROOT OF THE STARVATION FOUND (claim C024): the producer is only ever offered ONE candidate. Counted distinct item pointers inside the native producer over 6,000+ calls: distinctItems=1 for the whole replay. That candidate is item=0x800FC8D8, radius 80, at XZ (9950,5700), while G=0x800E7E80 is at (7227,3968) — about 3,200 units apart against a radius of 80. So the overlap test 0x8002300C is CORRECT to reject every time and nothing inside the producer is broken. The pump beams (ents puts their nodes at x=5562 and x=6678) are NEVER passed to it. This gives the earlier aux-list observation its right role: 0x801130C4 is the caller that would iterate MULTIPLE candidates, and its count at 0x1F800144 is zero all replay, so it contributes no calls at all — the 6,000 calls come from the other seven fixed-item callers. C023 remains falsified because it said that zero skips the PRODUCER; what it actually gates is whether the beams are ever CANDIDATES. NEXT: find what populates the candidate list at 0x1F80013C / its count at 0x1F800144. That is now the single question between here and a fix.

**2026-07-30:** 2026-07-30 UPSTREAM END LOCATED: the candidate list is the CULL QUEUE. The (list ptr at 0x1F80013C, count at 0x1F800144) pair is published by an adjacent mem_w32+316 / mem_w16+324 idiom, and scanning generated/ for that pair with the base provably 0x1F800000 finds exactly five publishers — 0x8007703C Cull::enqueueByClass, 0x8007712C Cull::decide, 0x80077E7C Cull::enqueueQueueA, 0x8003BB50 Render::objListWalk1, 0x800798F8 Pool::initTypedPools. ALL FIVE ARE ALREADY NATIVELY OWNED, so 'why are the beams not candidates' is now a question about Cull in readable C++ rather than about substrate. CHECK THIS FIRST: the publishers write the count as a HALFWORD (mem_w16 at +324) while the consumer reads it as a BYTE (mem_r8 at +324). Little-endian means a small count still reads correctly so it is probably benign, but it is unverified and a byte read of a halfword field is exactly the shape of the mem_r16s/mem_r32 signedness bug this card already fixed once. NOT established: which publisher should be enqueueing the beams, whether any diverges from its guest body, or what class filter they apply — nothing here says a Cull native is wrong.

**2026-07-30:** 2026-07-30 QUESTION CLOSED: the candidate list is CULL QUEUE A. Mechanism (guest and our native agree): cnt=mem_r16s(0x1F800144); if(cnt<24){ptr=mem_r32(0x1F80013C); ptr-=4; store ptr; store obj at ptr; cnt+1} — a downward-growing 24-slot push. Three such queues exist at stride 12: A 0x13C/0x144 cap 24, B 0x148/0x150 cap 40, C 0x154/0x15C cap 28. WHAT GATES ENTRY: Cull::enqueueByClass (0x8007703C) routes on the object CLASS BYTE obj[+0x0C] — class 4 to queue B, class 2 or 9 to QUEUE A, class 5 to queue C, everything else dropped with no push. The contact producer consumes queue A, so the candidate list is by construction 'visible class-2/9 objects, max 24'. OUR NATIVES ARE NOT THE BUG: performBaseCull/enqueueByClass/enqueueQueueA/enqueueVisibleClass4/enqueueQueueC all commit the push faithfully, checked line by line against the gen bodies (Cull::decide is a pure helper by design; performBaseCull is what is registered for 0x8007712C and it does push). The byte-vs-halfword count discrepancy I flagged is BENIGN and must not be re-chased: cap 24 means the halfword's low byte always reads correctly little-endian. NEXT, AND IT IS A CONTENT QUESTION NOT AN ENGINE ONE: what is the pump beams' class byte obj[+0x0C]? If it is not 2 or 9 they are dropped by design and the contact must arrive through a different mechanism.
