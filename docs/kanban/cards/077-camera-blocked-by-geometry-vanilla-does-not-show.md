---
id: 77
title: Camera blocked by geometry vanilla does not show — 2 spots, stage GAME (USER)
status: doing
labels: [bug, render]
created: 2026-08-06
updated: 2026-08-06
---

USER 2026-08-06, two spots, stage GAME 0x8010637C, coordinates are the RmlUi HUD camera triple (0x1F8000D2/D6/DA):
 (1) cam X 13029 Y -2872 Z 7161 — 'geometry blocking the screen ... that geometry IS there, but vanilla culls this'
 (2) cam X 20161 Y -1923 Z 8268 — 'the water way blocks the camera'
MECHANISM UNIDENTIFIED. Do NOT invent a distance/ID/alpha cull — that is a bandaid with no ground truth.
BLOCKED ON TWO INSTRUMENT DEFECTS, both measured 2026-08-06 (see the two sibling cards):
 - the report carries no AREA/SUB, and the HUD does not print one, so the coordinate cannot be resolved to a place;
 - psx_render draws no world geometry, so the pc-vs-psx reference comparison that would split 'vanilla culls it' from 'the object should be unloaded' cannot be run at all.

**2026-08-06:** MEASURED 2026-08-06 (agent). PHASE 1 LOCATED, both spots, 44-run sweep (2 targets x 22 areas, 42 ok, area 3 rc=139 both):
  T1 (13029,-2872,7161) -> AREA 13: green mass between camera and player. scratch/shots/blockcull-sweep/t1_a13.png
  T2 (20161,-1923,8268) -> AREA 14: wall of water filling the frame. scratch/shots/blockcull-sweep/t2_a14.png
  Coordinate-space finding: BOTH triples hold exactly (readback 800E7EAC unchanged after 300 frames) in EIGHT areas each {7,10,11,12,13,14,15,20}, and hold in X/Z with Y drifting in {2,4}. Elsewhere the teleport relocates. a9/a18/a19 are teleport-REJECTION artifacts (t1 and t2 land on the identical pixel), not repros.
PHASE 2 — MECHANISM NAMED, NOT YET IMPLEMENTED. Three hypotheses tested, TWO REFUTED BY MEASUREMENT:
  (R1) 'pc_render includes on node+1 while the guest uses the cull queues' — REFUTED AT THE SOURCE. The guest's own master render walk guest 0x8003C048 gates on mem_r8(node+1)!=0, exactly as render_walk.cpp does. node+1 is the guest's gate too.
  (R2) 'Cull::objectCull's margin re-include lowers the guest's near cull (0x200 -> 0x80) and puts blockers back' — REFUTED: Cull::objectCull HAS NO CALLER and NO tomba::native::declareOverride entry, and MarginRenderer::collect is called from nowhere else. The whole widescreen-margin re-include is DEAD CODE. (Filed separately.)
  (R3) THE LIVE ONE: the guest has NO RENDER WALK over HEADS[0] (0x800FB168). Its three render walks are guest 0x8003C048 over HEADS[1] (table 0x80014DB8), objListWalk4 over HEADS[2] (table 0x80015000), and guest 0x8003BB50 over the cull's QUEUE B (table 0x80014A70). HEADS[0] nodes reach vanilla's picture only via the cull's class-keyed queue push. render_walk.cpp flushes the whole HEADS[0] list instead — its own comment says 'table not yet RE'd — keep the flush-all behavior'.
EVIDENCE THE T1 BLOB IS A HEADS[0] NODE (break-first A/B, negative control = the pre-change binary): suppressing the HEADS[0] arm removes the green mass and NOTHING else in area 13 (2787/76800 px, scratch/shots/blockcull-ab/{before,after}_a13.png).
WHY THAT IS NOT THE FIX, also measured: (a) area 14's water wall is NOT a HEADS[0] node — the same suppression leaves it pixel-identical; the only a14 delta was 459 px of the PLAYER vanishing, so the arm carries legitimate content; (b) gating on queue membership cannot work — the queues are already drained/reset by display-pass time (live=45, queueCounters(A,B,C)=(0,0,0), 42 of the 45 are class 4, the very class that goes to queue B); (c) gating on the class byte removes nothing — 45/45 in a13 and 27/27 in a14 are already class 2 or 4.
REAL FIX = RE guest 0x8003BB50 (queue-B consumer, per-type table 0x80014A70, 144 entries) and reproduce its submission set for the HEADS[0] arm instead of flushing all. T2's water wall needs a separate producer trace (scene-table/terrain, dbgnode=FFFF0002 in PRIMAT) — different mechanism, same card.
LANDED THIS PASS: the PSXPORT_DEBUG=heads0 census only (no behaviour change; pre-change vs post-change is 0/76800 px in both scenes). NO invented cull was added.

**2026-08-06:** T2 TRACED 2026-08-06 (separate agent from the T1/HEADS[0] work). PRODUCER NAMED: Render::fxBackdropPlaneRender (game/render/fx_backdrop_plane.cpp), guest FUN_80110CA4 in the A0E overlay, node 0x800ED960 on HEADS[1] 0x800F2624, type +0xB=0x20 -> per-type table 0x80014DB8[32]=0x8003C29C (the RCASE_DEFAULT node+24 fn-ptr arm). It is kanban #48's area-14 waterfall backdrop. Break-first A/B, isolated clone, build exit status checked, legs distinguished by in-band channel counts: suppress fieldEntityRender (the scene table) = 6001/76800 px, bbox y[117..227], docks+barrels ONLY, wall untouched; suppress fxBackdropPlaneRender's drawWorldQuad = 63112/76800 px, bbox x[0..319] y[12..227], upper half BLACK. Shots scratch/shots/t2-producer/.
'dbgnode=FFFF0002 = scene table' was a PRIMAT FALSE POSITIVE: emitOrQueue's point-in-triangle test is true at EVERY pixel for a degenerate triangle and this path tees GT3s as quads with v3==v2. New tools/primat_filter.py (with --selftest) rejects those and prints denominators: at (160,40)/(40,20)/(280,90) frame 3940 -> 2 genuine covers each, ALL dbgnode=800ED960, vs 26-28 rejected degenerate hits. Instrument I046.
DOES VANILLA SUBMIT IT? YES at this camera, measured: FUN_80110ca4's own gates (GTE FLAG after RTPT+RTPS; two 'any vertex' screen tests; OT key = AVSZ4 -> +-0x50/0x78 bias -> grid-A near-snap -> log compression -> grid A drops k<4, grid B drops k outside [4,0x7FF]) reconstructed inside the native producer and COUNTED: 'quads=80 drawn=25+7 GTE-FLAG would drop 0 (divOverflow=0 sxSat=0 sySat=0 irSat=0); OT-KEY would drop 0'. NEGATIVE CONTROL, same run: f3043 15/32 dropped, f3192 28/38. So the gate fires when it should and reports 0 here. Same at T1: [otlink] in a13 = guest_DROPS=0 on all 8 nodes. NO CULL BELONGS HERE; any distance/ID test would be invented.
*** THE REPRO IS THE BLOCKER, NOT THE PRODUCER. *** The reported triple is the CAMERA readout 0x1F8000D2/D6/DA (overlay_glue.cpp:31-33); 'tp' writes TOMBA's master position 0x800E7EAC/B0/B4 (Engine::devTeleportApply). The sweep teleported the PLAYER to the CAMERA's coordinates. Worse, the 'confirm tp fired' check rw 800e7eac reads back the very word tp wrote (MASTER_X=G+0x2C, cutscene_camera.h:47) so it can NEVER report failure. And in area 14 nothing moves at all: tp 29940 498 6928 vs the T2 tp = 80/76800 px with a BYTE-IDENTICAL camera triple, and walking 240 frames leaves 0x800E7EAC unchanged. The camera the recipe actually reaches is (10382,-4344,9608) in a14 and (7455,-1510,7821) in a13 — NOT (20161,-1923,8268) / (13029,-2872,7161). The 'both triples hold in EIGHT areas' line in docs/findings/render.md rests on that circular readback and is withdrawn. Instrument I047, claims C041/C042.
NEXT: T2 needs a repro that can REACH a camera position (replay/pad drive searching the live 1F8000D2 readout), or the USER's area index. Not more producer archaeology.
NAMED UNPORTED GATE, measured NOT to be the cause here: submitPolyGt3Native/Gt4Native reproduce backface + right/bottom screen culls but not guest 0x8007FDB0/8008007C's two drops — 'if ((int32_t)gte_read_ctrl(31) < 0) skip' (twice) and 'if ((uint32_t)(k-4) >= 2044) skip'. game_sort_key already computes the second exactly and returns -1, but the caller treats -1 as 'no sort key' rather than 'the guest never linked it'. fx_backdrop_plane.cpp reproduces the FLAG cull only as pv.sz<=0, missing divide overflow and SX/SY saturation. Landing that is a real port of a real guest mechanism and must be gated on pixels in a scene where it FIRES (a14 approach frames: 15-28 quads), not on this bug. NO behaviour change was shipped.

**2026-08-06:** MEASURED 2026-08-06 (agent, RE pass on the HEADS[0] dispatch). THE HEADS[0] FLUSH-ALL IS NOT THE PRODUCER OF EITHER BLOCKER — and the psx REFERENCE draws both.
RE COMPLETE, all five per-type tables DUMPED FROM THE RUNNING GAME (scratch/logs/heads0/tbl_a13.log, parsed by scratch/parse_tbl.py): class(+0xC) -> queue -> consumer walk -> per-type(+0xB) table: 2/9->A->FUN_8003BB50 tbl 0x80014A70 (144 entries, no-op arm 0x8003BCD0); 4->B->FUN_8003BCF4 tbl 0x80014CB0 (33, no-op 0x8003BED8); 5->C->FUN_8003BF00 tbl 0x80014D38 (32, no-op 0x8003C028); any other class = queued by nobody. QUEUE B'S MISSING CONSUMER IS FUN_8003BCF4 (the old note saying guest 0x8003BB50 consumes queue B was wrong — it consumes queue A). Mesh arms: queue A types {0,15,64,79,128,143}, queues B/C {0,15}.
STEP-4 COMPARISON AT T1 = EQUAL. a13, PSXPORT_DEBUG=heads0: examined=45 (15 distinct nodes x 3 fieldObjectsRender calls), meshArm=45 (all type 0), onSubmitList=42. The ONE node the guest's own rule drops (800FB218, class 2, queue A, visible but never queued) is worth 0/76800 px.
IMPLEMENTED ANYWAY (it closes the named RE gap, the arm's own comment 'table not yet RE'd'): game/render/queue_dispatch.{h,cpp} models the chain read-only and Render::fieldObjectsRender's HEADS[0] arm applies it instead of the flush-all. 22-area sweep flush-all vs guest-rule: 0/76800 px in 21 areas, area 3 crashes identically (rc=139) on BOTH legs. Not vacuous — in a15 it drops 800EF53C (type 2 -> 0x8003BDF4, an area-overlay renderer, not the mesh flush).
POSITIVE CASE / NEGATIVE CONTROL: PSXPORT_ORACLE=1 (the psx reference leg, usable now that #78 is fixed) at T1 shows THE SAME green mass — blob-box mean RGB oracle (16,71,31) vs pc (20,73,32) vs HEADS[0]-suppressed (65,90,60), so the instrument does distinguish present from absent and both legs say PRESENT. Same at T2 for the water wall. The legs differ on 48933/76800 (T1) and 66861/76800 (T2) px overall, so the toggle demonstrably took effect. NEITHER BLOCKER IS GEOMETRY THE PORT INVENTED.
TWO INSTRUMENT DEFECTS FOUND AND FIXED: (a) 'the cull queues are drained by display time' is FALSE — the drained pair is the ACCUMULATOR; the first consumer walk of each frame SNAPSHOTS it into A 0x1F800140/146, B 0x1F80014C/152, C 0x1F800158/15E, and the snapshot IS the consumed list and survives to display time. Every earlier queueCounters=(0,0,0) reading sampled the accumulator. (b) A PUSH-SITE census in Cull is structurally blind under PSXPORT_GATE=1 — the whole cull family runs as the substrate there (PSXPORT_DEBUG=ovhit: Cull::cullWrapper native=0 / oracle=86368), so it printed nothing for 30 frames; the heads0 VERDICT line now prints nativeCullPushes and frameSubmitListEntries so that silence reads as an instrument fact.
NOT DONE / NEXT: the repro places TOMBA at the coordinates the user read off the CAMERA (0x1F8000D2/D6/DA) — '[tp] Tomba -> (13029,-2872,7161)' verified at 0x800E7EAC — so the measured viewpoint is NOT the user's. Reach the user's actual camera before more producer archaeology. Also RE'd but not modelled by the display arm: perObjRenderDispatch (FUN_8003CCA4) early-returns when (node[+0xD] & 11) >= 9 (table 0x80014EC8); at T1 every node has node[+0xD] in {0,1} so it changes nothing there.

**2026-08-06:** USER-REFUTED 2026-08-06: oracle at the ACTUAL spot (camera 13014/-2860/7237) shows NO mass — correctly culled. So a cull EXISTS and this port does not reproduce it. The EQUAL-branch conclusion was taken at the wrong viewpoint: the HUD triple is the CAMERA (0x1F8000D2), our tp moves the PLAYER (0x800E7EAC). Repro must drive the CAMERA and print the camera readback next to the target. The dispatch RE (5 tables, queue B consumer FUN_8003BCF4, accumulator-vs-snapshot) survives; the conclusion does not.
