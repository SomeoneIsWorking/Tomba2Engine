---
id: 47
title: pc_skip: entering 'House on the Point' corrupts state (music stops, camera unfollows, interior vibrates)
status: doing
labels: [bug, pc-skip]
created: 2026-07-23
updated: 2026-07-23
---

USER 2026-07-23, reported from their LIVE windowed game. Entering the 'House on the Point' interior corrupts the game state. NOT a freeze or crash — the game keeps running (frame advances ~30fps, no recomp-MISS, no abort). The corruption is BEHAVIOURAL / state, multiple symptoms at once:
  - the BGM/music STOPS on entry;
  - the house INTERIOR GEOMETRY VIBRATES (per-frame jitter);
  - on going back OUTSIDE, the CAMERA STOPS FOLLOWING Tomba.
This cluster (music + camera + geometry all wrong together) points at a single upstream cause — a corrupted scene/area-load or a state block clobbered on the interior transition — rather than three independent render bugs. RE the House-on-the-Point interior LOAD / scene-transition path and find what state it corrupts.

REPRO (cut from the user's live session with padrec): replays/bugs/house-on-the-point.pad, 4134 frames from boot, hand-played route entering the house. Replay headless: PSXPORT_PAD_REPLAY=replays/bugs/house-on-the-point.pad ./scratch/bin/tomba2_port (see replays/README.md).

VERIFICATION METHOD — this is a STATE bug, do NOT crash-check: a headless replay exits 0 while the state is wrong. Verify by observing the actual symptoms: (a) camera-follow — probe the camera-target/follow latch and whether it tracks Tomba's world pos after exit; (b) music — the BGM sequencer state (0x801054B0 open-seq / 0x80104C28 playmask, PSXPORT_DEBUG=seq) going silent on entry; (c) interior vibration — capture consecutive frames of the interior and diff for per-vertex jitter. Compare against a CORRECT house entry if one can be reached, or against the oracle (PSXPORT_GATE=1 + the same replay) to see whether the corruption is exec-state (present on the oracle too = a real state bug) or pc_render-only.
FIRST TRIAGE STEP: run the replay under PSXPORT_GATE=1 (recomp_path, the oracle) and see if the symptoms reproduce there. If yes, it is a genuine game-state corruption in the transition logic (highest priority, a pc_faithful/Job-#1-class bug); if the symptoms are pc_render-only, it is a render/producer issue.

**2026-07-23:** 2026-07-23 USER CORRECTION: this is a PC_SKIP-EXCLUSIVE bug, NOT a render bug. It reproduces under mPcSkip=true (the default ./run.sh config, which the user's live game runs) and NOT on the faithful path. So it is a Job #1-class fork defect: the House-on-the-Point interior LOAD has a pc_skip shortcut (game->mPcSkip ? load_in_one_step() : load_in_multi_step_faithfully()) that collapses a multi-step init and gets the END-STATE wrong — dropping whatever the faithful branch sets up: the BGM restart, the camera-follow latch, and the interior geometry/transform init (hence music stops, camera stops following on exit, interior vibrates — all three are state the collapsed init failed to establish).
TRIAGE (corrected — the fork is pc_skip, not GATE): reproduce with the default (mPcSkip=true) on replays/bugs/house-on-the-point.pad, then run the SAME replay with the FAITHFUL branch forced (PSXPORT_SKIP=0 / the pc_skip=false path — see docs on how run.sh selects it) and confirm the symptoms vanish. That isolates it to the fork. Then SBS-full (both cores mPcSkip=false is the byte gate; but here the DIVERGENCE is skip-vs-faithful, so use MODE=skip positive-observable compare, or diff the state the faithful branch sets that the skip branch omits). RE the House-on-the-Point interior init fork and add the missing steps to load_in_one_step (or route the affected sub-inits through the faithful path). Reference the pc_skip fork rules: no inline if-else (feedback_no_pc_skip_inline_blocks), collapse must bump tick counters (feedback_pc_skip_frame_counter_bump), never route pc_skip=0 to substrate. The camera-follow latch and BGM sequencer state are the two most probeable end-states to check first.

**2026-07-23:** 2026-07-23 INVESTIGATION (agent): root cause NAMED, fix NOT shipped (see docs/findings/scene.md 'kanban #47 ... parked in the mid-transition state sm[0x4c]==3'). Three corrections to this card: (1) the VIBRATION is fps60 interpolation, not the fork — with fps60=0 consecutive interior pad-frames diff 0/76800 px, with fps60=1 they diff ~14400 px (18.7%) every frame with a completely idle pad, and it happens OUTSIDE too (24.7-35.7%); it belongs to the fps60 lerp debt (#31/#33). (2) the MUSIC stop is real and precise — wav RMS goes to exactly 0.0 at t=22s = the exact frame sm[0x4c] goes 2->3 (f661); it is a fade-out (spu_render peak 10399->1894), SPU main vol/CDVOL and the libsnd seq state are unchanged; driving Tomba back out with the REPL returns sm[0x4c] 3->2 and THE MUSIC COMES BACK, so nothing in the audio stack is broken. (3) the real defect: sm[0x4c]=3 is written by gen_func_80026AD0 (screen-transition sequencer) from ov_a00_gen_801167AC via Array8Dispatch::tick (proven by a PSXPORT_CW_BT host backtrace on 0x801FE04C), and the game NEVER LEAVES sm[0x4c]==3 — 3472 frames in the reduced mid-transition body Engine::fieldFrameX, which by design drops sceneStateStep/areaModeDispatch and the full object walk. That is the already-flagged 'FUN_80026ad0 not ticked to completion' gap. NEXT RE STEP: Ghidra on gen_func_80026AD0 + ov_a00_gen_801167AC + FUN_8007B04C. BLOCKER for the fork gate: PSXPORT_PC_SKIP=0 and PSXPORT_GATE=1 advance the field at ~half the pad-frame rate and never reach the house on this capture (a 2x-stretched pad also fails — the faithful path stalls Tomba at X~9550), so frame-indexed pad replays are NOT portable across the pc_skip fork; REPL 'gate on' is not an in-place oracle either (resets the stage machine, recomp-MISS 0x80109450).

**2026-07-23:** 2026-07-23 (agent, RE step + PARTIAL fix): the completion chain is now fully RE'd end to end — sm[0x4c]=2 is written only by gen_func_80026AD0 case 4, gated on bf818==3, whose only writer is SceneTransition::completeSwap from stepSwapWaiter case 3, whose gate needs door[+0x29]!=0, set only by gen_func_80020868 when door[+0x5F] != G[+0x147] (Tomba FACING INTO the door). Measured: waiter case 2 flips door[+0x5F] 1->0, so completion needs G[+0x147]==1 — Tomba turned around — and nothing turns him. FIXED one of the two broken links: the area-0 transition-ENTER hook (FUN_8006C77C -> ov_a00_gen_8010CB60, run by Engine::fieldRunX state 0) was being TRUNCATED — it parks on a cooperative FUN_80044BD4 spawn-and-wait, and under pc_skip the GAME task is a FLAT setjmp task so scheduler_yield longjmps back to runGameStanza and discards the stack, so the hook tail (G[5]=36, the Tomba door-transit state that hands the facing over) never ran. Proof: PSXPORT_DEBUG=sched prints 'caught a GAME substate yield' at f661, the hook's post-spawn store 0x1F80019C=0 never fires, and a byte watch on G[5] shows no write of 36. Ported the hook natively with a synchronous load (game/scene/scene_transition.cpp, wired from fieldRunX's pc_skip branch only; pc_faithful untouched — it runs on the stage fiber where the yield resumes). After the fix G[5]=36 fires and Tomba walks through the door. STILL PARKED though: the facing hand-off is a no-op for this door (0x800BF81F is stamped (1-door[0x5F])<<4 BEFORE the flip, so state 36 loads exactly the post-flip value and the gate stays shut). NEXT RE STEP: find what puts Tomba into state 4 (the 180-degree turn, gen_func_800645E0/80063158) on a swap-key-1 door — note ov_a00_gen_8010CB60's key==2 branch DOES rotate G[0x140] by 2048, key==1 does not. Dead ends recorded in docs/findings/scene.md ('kanban #47, part 2'): bf818==6 has no writer; holding a direction does not help (camera masks never flip); the +0x28&0x80 walkOnce gate is FALSIFIED as a suspect (door node is 0x81, walkOnce dispatches it every frame); stepSwapWaiter is subswapverify 0-mismatch.

**2026-07-23:** 2026-07-23 PARTIAL — integrated (commit 9f588b2), NOT closed. The USER'S 'pc_skip exclusive' CLASSIFICATION IS CONFIRMED AND EXPLAINED: under pc_skip the GAME task is a flat setjmp task, so the area-0 transition-ENTER hook's cooperative FUN_80044BD4 wait longjmps out and DISCARDS the C++ stack — its tail (which sets Tomba's door-transit state G[5]=36) never ran. pc_faithful runs GAME on the stage fiber where the yield resumes, hence pc_skip-only. Filed as SYSTEMIC card #50 (areas 1..21 carry the same truncation; the general fix is PcScheduler::spawnAndWait running the body inline, which would also let SEVEN open-coded collapses be deleted).
WHAT THE PARTIAL FIXED: native port of FUN_8006C77C + 0x8010CB60 with the cooperative wait collapsed synchronously, wired from fieldRunX's pc_skip branch. G[5]=36 now fires at f661 and gen_func_80065A54 runs (Tomba walks through the door). Verified no regression: dialog opaque+legible at bucket-softlock f1200, field renders, boot smoke 0 misses, dup-installs 0, BGM RMS unchanged across the transition (the collapse did not break audio).
WHAT REMAINS (the residual, narrow and specified): sm[0x4c] still parks at 3. The full completion chain is now RE'd end to end — sm[0x4c] leaves 3 only via gen_func_80026AD0 case 4, reachable via case 3 gated on bf818==3, written by SceneTransition::completeSwap, gated on door[+0x29]!=0 && G[+0x29]!=0 && *(u8*)0x800E7FFB==0; door[+0x29]'s only non-zero writer is gen_func_80020868 whose final gate is door[+0x5F] != G[+0x147] ('Tomba faces INTO the door'). stepSwapWaiter case 2 flips door[+0x5F] 1->0, so completion needs G[0x147]==1 (Tomba turned around). The facing hand-off is a NO-OP for this door: 0x800BF81F is stamped (1 - door[0x5F]) << 4 BEFORE the flip, so state 36 loads the post-flip value and the gate stays shut. NEXT RE STEP (narrow): find what puts Tomba into state 4 (0x80015CC4[4] -> gen_func_800645E0 -> gen_func_80063158, the 180-degree turn — the only other writer of G[0x147]) on a swap-key-1 door. Note 0x8010CB60's key==2 branch rotates G[0x140] by 2048; key==1 does not, and bf817 is the door index (node[3]), not a transition type.
FALSIFIED SUSPECTS (dead ends, do not re-chase — in docs/findings/scene.md 'kanban #47, part 2'): the +0x28 & 0x80 gate in walkOnce (door node is 0x81, dispatched every frame); bf818==6 (no writer anywhere, re-confirmed across MAIN + all overlays); stepSwapWaiter itself (debug subswapverify = 950+ matches, 0 mismatches). NO BANDAID SHIPPED: no sm[0x4c]=2 write, no timeout.
