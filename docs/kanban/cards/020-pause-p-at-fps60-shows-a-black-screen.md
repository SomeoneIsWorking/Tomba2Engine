---
id: 20
title: Pause (P) shows a black screen at fps60 — the pause loop RE-RENDERED the frame instead of re-showing it
status: done
labels: [render, fps60]
created: 2026-07-22
updated: 2026-07-22
---

USER 2026-07-22: pressing P to pause while fps60 is ON shows a BLACK SCREEN. At 30fps pause presumably holds the last frame correctly (user reports this as an fps60-specific fault). MECHANISM TO CHECK FIRST: fps60 presents by re-running the world pass one frame behind under lerped input chokes into an isolated sink, then merging with verbatim 2D (see the fps60 entry in docs/findings/render.md). A PAUSE stops the game advancing, so the interpolator's two endpoints stop being refreshed - if the paused present keeps consuming the interp path it will lerp between a stale pair, or between a valid frame and an EMPTY sink, and an empty sink presents as black. So the suspect is the pause path presenting through the interpolated pass instead of re-presenting the last REAL frame. Related known code: the debug/pause loop calls PadInput::pumpHostInput rather than serviceFrame precisely so the pad clock does not advance while frozen (external/psxport/runtime/recomp/native_boot.cpp) - check whether the fps60 present is correspondingly frozen, or whether it keeps running with nothing feeding it. Repro: run windowed or drive the pause via the debug server, toggle fps60 on, pause, shot. Fix must NOT be an anchor/stamp special case (banned by the CLAUDE.md lerp clause) - the correct behaviour is that a paused frame is simply the last real frame presented through the same path.

**2026-07-22:** 2026-07-22 MECHANISM ESTABLISHED by code reading (operator), fix not yet implemented. The debug-pause loop in external/psxport/runtime/recomp/native_boot.cpp (~line 421) keeps the window alive by calling GpuState::gpu_repaint() every iteration. gpu_repaint -> present_window() -> blit_src(s_vram, s_disp_x, s_disp_y) -> gpu_vk_present(core, src, ...) — i.e. it re-presents the PSX VRAM FRONT BUFFER. That is the psx_render picture. Under pc_render the picture is not in s_vram at all: it is produced by the VK geometry rasterizer through GpuState::gpu_present_ex / gpu_fps60_present_pass (gpu_native.cpp:1276/1423). So pausing re-presents VRAM, which under pc_render is not the frame — hence black. PREDICTION TO TEST FIRST, because it changes the card's title: this should reproduce at 30 fps too, i.e. it is a pc_render bug and not an fps60 bug; the user simply hit it at 60. Verify before fixing. THE FIX SHOULD USE WHAT NOW EXISTS: as of 2026-07-22 both presents go through Fps60::presentPass(core, t) and the real frame IS presentPass(core, 1.0f), so 're-present the current frame without advancing logic' has a natural single expression — re-run the same pass, or re-present the VK target it filled. Do NOT add a third present path; that is the two-code-paths bug (#17) which was just removed. NOTE the comment on gpu_repaint claims 'the VK readback reflects exactly what is on screen (vkshot reads the same region this presents)' — if that is wrong under pc_render then vkshot while paused is also lying, which would matter for every paused-state investigation. HARNESS WARNING, cost me several attempts: driving a pause headlessly is awkward. PSXPORT_REPL=1 with no stdin blocks the game so the debug server times out; without REPL the frame loop runs to completion and exits. Use PSXPORT_DEBUG_SERVER=1 and keep the process alive properly, then dbg commands 'pause' / 'vkshot <path>' / 'frame' on 127.0.0.1:5959. Note tools/dbgclient.py referenced in the docs DOES NOT EXIST — either it was deleted without updating docs/driving-the-game.md (a tombstone to purge) or it needs writing; that is a workflow defect to fix in the same session per WORKFLOW FIRST.

**2026-07-22:** CORRECTION to the note above: dbgclient.py DOES exist — at external/psxport/tools/dbgclient.py. It moved there in the framework/game repo split and two parent docs still pointed at the old top-level tools/ path (docs/gfx-debug.md, docs/render-arch.md); both fixed. docs/driving-the-game.md already had the right path. Use: python3 external/psxport/tools/dbgclient.py <cmd>.

**2026-07-22:** FIXED 2026-07-22. The 30fps PREDICTION IS FALSIFIED: measured headless, 30fps paused == running (mean 78.30, 76793 non-black px), 60fps paused == fully black (mean 0.00). So it is not 'pc_render's picture isn't in s_vram' — present_window() does not blit s_vram, it calls gpu_vk_present which RE-RENDERS (upload_vram + render_geom over the LIVE vertex batch). ROOT CAUSE: the pause repaint REBUILT the frame, and whether that worked was an accident of frame ordering. render_geom clears s_vram_tex to black when the batch is empty ('no native submit -> the PC renderer shows BLACK'). With fps60 OFF, frameUpdate presents at the TOP of the frame and drawOTag/rq.flush fills the batch at the BOTTOM, so a full batch is still resident at the frame-loop top where the pause loop sits — the re-render accidentally reproduced the picture (while re-rendering the whole world every 15ms). With fps60 ON, Fps60::present_vk emits+presents+frame_end-resets BOTH passes inside frameUpdate and rq.flush only rq_capture()s afterwards, so the batch is EMPTY -> the clear fires -> black. CONSEQUENCE CONFIRMED: vkshot while paused WAS lying — the clear writes s_vram_tex, the exact texture readback_vram/dump_to download; at fps60 every paused readback returned black. Comment corrected. FIX: split the swapchain half of GpuVkState::present into show_composite(cmd) and add GpuVkState::repaint() = acquire cmd + show_composite — re-SHOW the finished composite, no upload, no geometry, no batch reset. One authority for building a frame is preserved (no third present path, cf #17). VERIFIED headless both modes: pause, ~200 repaints, step 1 (real frame) -> vkshot R, ~200 more repaints -> vkshot R'; R'==R exactly (maxdiff 0) at fps60=1 AND fps60=0; paused mean 78.32/78.24 (was 0.00); step 1 advanced the counter by exactly 1 and changed the picture. RESIDUAL: show_composite is a pure code move not exercised on a real swapchain (no window available; Xvfb has no DRI3 so Vulkan refuses presentation). Details: docs/findings/render.md; harness recipe: docs/driving-the-game.md #HEADLESS INTERACTIVE.
