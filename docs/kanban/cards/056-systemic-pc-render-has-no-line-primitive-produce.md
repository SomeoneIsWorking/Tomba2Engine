---
id: 56
title: SYSTEMIC: pc_render has NO line-primitive producer — every GP0 line is invisible (rope, fishing line)
status: done
labels: [render, bug]
created: 2026-07-23
updated: 2026-08-06
evidence: docs/reference/issues/issue56_fisherman_line_missing.png
---

SYSTEMIC. USER 2026-07-23 reported the bucket's ROPE (#54) and a fisherman's fishing LINE missing under pc_render, in separate scenes. They are the same defect, and the cause is structural rather than per-effect.

MEASURED (grep, 2026-07-23): the GP0 interpreter HANDLES line primitives — gpu_native.cpp:991 `else if (op >= 0x40 && op <= 0x5F)` covers flat/gouraud single lines AND poly-lines, collecting an N-vertex list and drawing each segment. That is the psx_render path, which is why lines appear under PSXPORT_RENDER_PSX=1 and in the user's reference captures.
But the NATIVE RENDER QUEUE HAS NO LINE PRIMITIVE AT ALL: render_queue.{h,cpp} and game/render/submit.cpp contain no line path — the queue deals in triangles/quads (nv=3/4) via emitOrQueue / drawWorldQuad / push2dQuad. Since the break-first rebuild pc_render does not walk the guest OT, every GP0 line the guest emits is simply never drawn under pc_render.

SO THIS IS ONE GAP WITH MANY SYMPTOMS, not a series of missing effects. Anything thin and line-drawn is invisible: the bucket's suspension rope (#54), the fisherman's line, and plausibly other cables/wires/chains/whiskers not yet noticed. Expect more sightings; they should be attached here rather than filed separately.

THE FIX: give the render queue a LINE primitive and a native producer for it.
  1. RE the emitters. `debug otattr` on a scene with a visible line (the fisherman, or the bucket) attributes the op-0x40..0x5F packets to their emitting fn AND caller. Confirm whether they come from one shared line emitter or several.
  2. Add a line kind to the queue. A PSX line is a 1px-wide segment between two screen points with flat or gouraud colour and optional semi-transparency. Simplest faithful native form is to expand each segment host-side into a quad (2 triangles) of the correct width in screen space, submitted through the existing emitOrQueue path with real depth — no new rasterizer needed. Keep the expansion in the PRODUCER, not in the queue, so the queue stays quads-only.
  3. PORT the emitters per the USER directive (a tap is not acceptable): read the endpoints from the emitting object's own state, project natively with projComposeCamera(sceneCam) so the line lerps at fps60, emit with has_xyf=1. game/render/fx_sprite.cpp and fx_dust.cpp are the templates for a type-0x20 node producer.

WATCH OUT — a known past defect in this exact area: docs/findings (memory `tomba2-gfx-fixed-polyline`) records that gp0_len once MISSED variable-length poly-lines, walking off the end and clobbering VRAM, and that a differ running on corrupt VRAM then reported a FALSE 'faithful'. Poly-lines are variable-length (terminated, not counted). Do not assume 2 vertices.

VERIFY against a SECOND run with PSXPORT_RENDER_PSX=1 at the same frame index — never a bare mid-scene `renderpsx` toggle (kanban #41 proves it silently returns a pc frame).

**2026-07-23:** 2026-07-23 ROPE + TETHER PORTED (operator-integrated + verified). Census via a new `lineprim` diagnostic over bucket-softlock f0-445 found EXACTLY THREE line emitters, not one: FUN_8013E9D8 (554 pkts, the bucket ROPE), FUN_80122974 (64 pkts, the TETHER/fishing line), FUN_8013E08C (1064 pkts, the ground RING SHADOW). Two of the three share one leaf — FUN_8013DD34, 'draw a rope between two WORLD points': builds the midpoint, RTPTs (A,M,B) together, emits a 4-point corner-cut gouraud poly-line so a rope CURVES instead of kinking, colour from a 16-entry grey ramp at 0x8014BD04 indexed (SX0+SY0)&0xF, drawn twice one pixel apart for a 2px stroke, blend mode 3 (B+F/4) measured by instrument rather than decoded. Ported as Render::worldLineDraw (game/render/fx_line.cpp): projects through projComposeCamera and expands each segment into a screen-space quad IN THE PRODUCER, so the queue stays quads-only — no new rasterizer, no gen_func, no gte_op, no guest write. Gates: native endpoints (214.7,-24.4)..(197.7,68.9) vs guest packet (214,-24)..(197,69); A/B isolation 130px at f443 (rope) and 69px at f100 (tether) with changed pixels exactly dst+F/4 confirming blend 3; fps60 lerp 5/5 interps strictly between real neighbours. OPERATOR-VERIFIED after integrating alongside the #61 SBS fix: producer fires 607 times at f443, SBS-full 0-diff through f16830, dialog + item menu clean, dup-installs 0. STILL OPEN: the ring shadow (FUN_8013E08C) is the same #56 gap but a DIFFERENT emitter with its own GTE loop over a 16-point circle at 0x8014C780; porting it needs FUN_80084110/80084220 RE'd first, so it was correctly NOT guessed. Filed as portmap step world-line-ring-shadow.

**2026-08-06:** 2026-08-06 (G10 survey) — THE LINE CLASS IS CLOSED; this card can be re-read as done pending a USER eyeball. Its last open item was the ring shadow FUN_8013E08C, which portmap step world-line-ring-shadow still listed as todo/BLOCKED. It is in fact ported: the SAME address is step fx-line-emitter-e08c, landed 2026-07-28 as Render::shockwaveRingRender (fx_line.cpp), whitelisted in fieldObjectsRender, and codemap --addr 0x8013E08C returns it LIVE. MEASURED, same replay, two legs: psx_render emits 1658 GP0 line packets over 460 frames (1064 op-0x4A ring + 594 op-0x5E rope/tether, PSXPORT_DEBUG=lineprim); pc_render emits 0 line packets and instead fires the native producers 152 (shockwave) + 598 (rope leaf) times (PSXPORT_DEBUG=ropeline). NEGATIVE CONTROL: the same lineprim instrument reported 1658 on the psx leg of the same run pair, so it is not a silent instrument. STILL UNVERIFIED: which visual FUN_8013E08C actually is — this card and the port-map call it a ground ring shadow, fx_line.cpp calls it an expanding shockwave ring, and nobody has looked. Also un-root-caused: 152 producer calls is exactly 2x the 76 the guest packet count implies.

**2026-08-06:** 2026-08-06 PIXEL GATE — the close now rests on PIXELS, not on a primitive count. The 2026-08-06 entry above closed this card on '0 line packets on the pc leg, 598+152 native producer calls'. That is a COUNT, and a count cannot tell a producer that drew from one that emitted into a batch nobody rendered — the exact mistake this repo already has a claim for. So the gate was redone as pixels.

MEASURED (claim C035, evidence scratch/lineclass/evidence/present_44*_OFF_ON_DIFF.png): four separately-built binaries from an isolated tree (distinct md5s, each build exit-0 checked, never the shared checkout), same replay bucket-softlock.pad, same frames, headless, PSXPORT_GATE=1 pc_render, PSXPORT_PRESENT_SHOT_AT -> gpu_vk_present_shot 960x720.
  leg A rope producers live vs leg B them deleted: 1584 / 1197 / 927 changed px of 691200 at presents 440/445/450, bbox 41x263 / 41x206 / 29x164 — tall, thin, moving right as the camera pans. The diff mask is one continuous curved line: it IS the rope.
  IN-BAND LEG PROOF: both legs log the same producer invocation counts (666 worldLineDraw calls, 152 shockwave), so both reached the same scene and only the drawing differs.
  NEGATIVE CONTROL: the same instrument in the same mode reports exactly 0 changed pixels on presents 455-485, where the producer is not called at all (last call f453, and the call count independently saturates at 666 by f456).
  SENSITIVITY: a third leg with a 12x-wider white stroke gives 19107 px = 12.06x, bbox +66 px = 22px x ires 3. The response scales as the geometry predicts.

NO FOURTH EMITTER (claim C034): the lineprim census this card asked for in step 1 was only ever run on ONE replay. Re-run over the WHOLE 16-replay library on the psx leg: 97,516 line packets, 0 unattributed, exactly THREE emitter addresses (0x8013E9D8, 0x80122974, 0x8013E08C), all three with a LIVE native producer. Harness validated first against the recorded 1658/1064/594 for bucket-softlock.

STILL OPEN, and this card should NOT be read as covering them:
  * Render::ropeChainRender (0x8013EA64) is COLD — it does not appear as an emitter in ANY of the 16 replays, so the chain half of this card has never been exercised in either renderer. Implemented, never seen.
  * ~~Render::shockwaveRingRender (0x8013E08C) contributes ZERO pixels (claim C036): 152 calls, 0 px, node at world (7,200,10) while the camera is at (10264,...) — consistent with off-screen, so cold rather than broken.~~ **THIS LINE WAS WRONG — CORRECTED 2026-08-06 (see the entry below). It was BROKEN, not cold, and it is now fixed and pixel-verified.**
  * POSITION was not checked against psx_render pixels in this pass — only presence/absence. And still unresolved from 2026-08-06: which visual FUN_8013E08C actually is (this card says ground ring shadow, fx_line.cpp says expanding shockwave ring).
  * Frame-cap blind spot: 4 of the 16 replays were capped at 6000 frames, and the library is not the 22-area sweep.

Moving to done on the rope/tether family per the amended close rule (USER 2026-08-05: close what you believe is solved, reopen if not). The chain and the ring are tracked by C036 and the inventory row, not by leaving this card open.

**2026-08-06:** 2026-08-06 THE RING WAS BROKEN, NOT COLD — FIXED AND PIXEL-VERIFIED. The 'STILL OPEN' line above (shockwaveRingRender contributes ZERO pixels, 'consistent with off-screen, so cold rather than broken') is struck through because it was FALSE, and the way it was false is the recurring failure this card already documents once: the A/B behind it captured presents 440-485 while the producer fires only at f270..f358, so the window contained zero of its calls. C036 was rewritten to status:falsified for that; this entry closes it.

ROOT CAUSE — TWO independent bugs in Render::shockwaveRingRender, either fatal alone, both found by RE-ing the emitter's own instruction stream (generated/ov_a00_shard_0.c ov_a00_gen_8013E08C) rather than by black-boxing:
  (1) WRONG TRANSLATION SOURCE. FUN_8013E08C passes node+0x2C to 0x80084220, which loads word0 as VXY0 and word1 as VZ0 — a packed SVECTOR, X@0x2C Y@0x2E Z@0x30. The producer read node+0x4E/0x50/0x52, the ROPE/TETHER node family's layout. On a ring node 0x50 is the SCALE animator, so the port's Y was literally the ring's own radius and X/Z came from unrelated fields — which is exactly why the logged 'position' walked (0,30,10)..(7,210,10) in lockstep with the scale. The real position on this replay is (6861,-756,3968), next to the camera. The other ported type-0x20 node renderer, narrationSwirlRender, already read node+0x2C the same way.
  (2) Robj SCALED DOWN BY 4096. projComposeObjectHost takes the object rotation in the guest's 1.3.12 convention (4096 = identity); the producer divided scale<<4 by 4096 first, collapsing every ring to a single point. Only visible once the position was fixed AND the producer logged its own projected screen box with a denominator — the old one-line diagnostic could not have shown it.
ALSO CORRECTED: the guest emits each span TWICE with its OWN DR_MODE (0x80083DE0 tpage 53 -> blend bits 1 = B+F additive, unshifted; tpage 85 -> blend bits 2 = B-F subtractive, at (+2,+1), inserted so it draws underneath) — an embossed highlight+shadow pair, confirmed by the lineprim census's own alternating blend=1/blend=2 and the (+2,+1) delta between consecutive packets. The producer had both copies on blend 3.

GATE (claim C038). Isolated tree, three separately-built binaries with distinct md5s, each build exit-0 checked, in-band leg proof (off: 152 LEGOFF marks / 0 producer calls; old and new: 152 calls / 0 marks). replays/bugs/bucket-softlock.pad, PSXPORT_GATE=1 pc_render headless, PSXPORT_PRESENT_SHOT_AT=275,280,287,320,340,355 — all INSIDE f270..f358. fixed-vs-deleted = 450/657/909/2151/1602/2232 changed px of 691200. NEGATIVE CONTROL: the SHIPPED producer vs the same deleted leg, same presents, same differ = 0 on all six. Every diff mask is a single closed ellipse outline with no stray region anywhere on screen, and the native producer's projected box tracks the guest's own lineprim packet vertices to ~1px on 8 frames.

TWO OF THIS CARD'S OWN OPEN ITEMS ALSO CLOSE. Which visual FUN_8013E08C is: an EXPANDING SHOCKWAVE RING, not a ground ring shadow — it grows from ~6px across at f270 to ~80px at f357 while its grey fades 122->13, three instances over f270..f358. And the 152-vs-76 factor of 2 is NOT a bug: 152 = 76 distinct (frame,node) pairs seen TWICE, one per PRESENT, because fps60 re-renders the field object walk for the interpolated present — with fps60=0 the same replay logs exactly 76, matching 1064 packets / 14 per call.

STILL NOT COVERED: Render::ropeChainRender (0x8013EA64) is still COLD in all 16 replays; one replay / one area / six presents here; and no USER has eyeballed the ring.
