---
id: 32
title: 17 guest addresses have two native owners — the class that caused #28
status: todo
labels: [workflow]
created: 2026-07-22
updated: 2026-07-23
---

tools/codemap.py --conflicts reports 18 cross-file duplicate-owned guest addresses; one of them (0x8004FFB4) caused #28 today — the dialog box drawing over its own text — and is now down to 17. This is a nasty class because NOTHING catches it automatically: a second owner does not fail the build, does not fail SBS (both owners write the same guest state, so the byte-compare stays green), and surfaces only as a picture bug or a subtle behavior change days later. codemap --conflicts is the only detector. Two things to do. (1) Triage the remaining 17 — each is either a deliberate pc_skip fork (those are supposed to live in ONE file, so a cross-file pair is never that), a stale ORPHAN transcription that should be deleted per no-tombstones, or a live duplicate like 0x8004FFB4. Known-suspicious from the listing: 0x8007FCC8 Panel::pushDialogBackdrop vs Render::optionsSolidBox, 0x80115598 Render::backdropRender vs TileGridLayer::emit, and a cluster of Render::options* vs leaf_* in game/core/field_owned_leaves.cpp. (2) Make it impossible to reintroduce: wire codemap --conflicts into a pre-commit check the way codemap.py check already flags coverage drift, so adding a second owner FAILS instead of shipping.

**2026-07-23:** 2026-07-23 THE DETECTOR IS NOW AUTOMATIC (USER: 'overriding the same address twice should cause a crash imo'). overrides::install aborts when a second install changes the owner of an address (external/psxport d2c3c64b); re-installing the SAME handlers stays idempotent so self-guarding call sites are unaffected. That closes the reason this card said the class was invisible — it is no longer detectable only by running codemap --conflicts and reading it. Swept clean at the time of arming: boot, triangle menu, START page, bucket dialog replay and the pure-oracle leg all run with zero conflicts. WHAT REMAINS ON THIS CARD is the OTHER half, which the abort does NOT cover: codemap --conflicts still reports 19 cross-file duplicate-owned addresses, and those are SOURCE-level ownership claims (two files each RE'ing and owning the same FUN_xxxx), not runtime double-installs — an ORPHAN transcription that nobody installs will never trip the abort but is still duplicated code drifting out of sync, and per no-tombstones should be deleted. Triage those 19 by hand.

**2026-07-23:** 2026-07-23 CORRECTION — my earlier 'armed and swept clean' claim was made against a STALE BINARY and is FALSE. My build-check filter was grep -E ' error ', which cannot match gcc's 'file:line:col: error: ...' (no space before the colon), so a failing build looked identical to a passing one and every run afterwards used the previous binary. Once the filter was fixed (grep -E 'error:' plus the build's real exit status) the very first honest run FIRED THE NEW ABORT: guest 0x80049760 already owned by 'Collision::flatNormal' (game/player/collision.cpp:1036, the properly RE'd owner), newcomer 'leaf_80049760' installing from register_field_owned_leaves — two different native pointers, same gen. So the guard works and there is at least one REAL pre-existing duplicate in the tree, which the stale-binary sweep had reported as clean. TO CONFIRM once the working tree is stable (an agent is mid-edit in external/psxport and the link is currently broken by its in-flight change): re-run boot, re-read the fatal, then delete the stale leaf transcription rather than the real owner — per no-tombstones an orphan transcription of an address someone else properly owns should not exist at all. Then re-do the sweep HONESTLY (boot, triangle menu, START page, dialog replay, oracle leg) and only then claim it clean. LESSON, and it is the fourth broken instrument in two days: a filter that cannot express the failure it screens for reads exactly like success. Check the exit status, not just a grep.
