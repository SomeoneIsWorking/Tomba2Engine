---
id: I029
kind: instrument
status: trusted
created: 2026-07-30
---

## Instrument

codemap.py --unowned-rank / --addr — PlatformHle-aware ownership (supersedes I028)

## Validated by

Run against BOTH classes on the live tree. POSITIVE (must be reported owned): --addr 800834A0 -> 'OWNED by PlatformHle as gpu_timeout_arm()', --addr 80085900 -> 'vsync_trap()'. NEGATIVE (must still report unowned, with its denominator): --addr 80086288 -> 'NO native owner found. (searched: 986 scanned natives, 9 PlatformHle entries. Blind to: overlay/interpreted-only code with no native def.)'. CONTROL (normal scanned native unaffected): --addr 800753d4 -> preload_cel [LIVE]. QUEUE: --unowned-rank now heads at 0x80040558 17878 and prints 'Excluded 1 PlatformHle-owned address(es) ... 0x800834A0 (33152 hits, gpu_timeout_arm)' instead of listing it #1. REFUSAL PATH TESTED (the table cannot go silently empty): registrar file missing -> 'could not read registrar ...'; config file missing -> 'could not read config ...'; registrar shape changed -> 'no reg(h.<field>, ...) entries parsed'; each surfaces as a WARNING on --addr and 'Treat every entry above as UNVERIFIED ownership' on --unowned-rank. Markdown output proven unaffected: docs/code-map.md regenerated by HEAD's tool and by the patched tool, run as subprocesses at equal directory depth, is byte-identical (diff -q clean).

## Known failure modes

(none recorded yet)
