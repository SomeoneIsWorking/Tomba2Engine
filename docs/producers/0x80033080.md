---
key: 0x80033080
kind: guest
first_seen: 2026-08-11T22:42:49
last_seen: 2026-08-19T19:50:10
runs: 99
prims_guest_max: 0
prims_native_max: 280
frames_seen: 44
gte_calls_max: 0
has_native: false
native_reached: false
native_hits: 0
oracle_hits: 0
name: weapon impact effect (controller)
re_status: ported-partial
re_evidence: kanban #15; FUN_80033080 = { FUN_80027E5C(); FUN_800288AC(node); }. docs/re/impact-plume-288ac.md decodes the mesh half.
producer_file: game/render/fx_sprite.cpp
partial_because: Only the SPRITE half is ported. The MESH half is FUN_800288AC, fully RE'd in docs/re/impact-plume-288ac.md but NOT ported: it needs meshQuadRecordsEmit's CLUT-row bias (now landed) and then a producer.
compare_status: never-compared
notes: Observed 64 prims over 10 frames on bucket-softlock — that is the sprite half alone.
---
## What it is

NOT YET IDENTIFIED. This row was created mechanically the first time this producer submitted geometry;
nothing has been reverse-engineered about it yet. Fill `name` and the `re_status` chain above as it is
worked, and write here what the effect actually IS — where it appears, what drives it, what it reads.

## Native producer

None recorded. If `has_native` says true, the override table owns this address; name the file in
`producer_file`. If it is false, this effect's picture still comes from the guest.

## Evidence

(Decompile with `external/psxport/tools/decomp.sh`, then cite it here. A `re_status` above `unknown`
with no `re_evidence` is a lint error — see `tools/producers.py check`.)
